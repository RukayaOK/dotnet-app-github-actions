name: Call a reusable workflow

on:
  pull_request:
    branches:
      - main

jobs:
  call-workflow-passing-data:
    uses: https://github.com/RukayaOK/reusable-actions/blob/main/.github/workflows/dotnet-ci.yml@main
    with:
      DOTNET_VERSION: 6.0.x
      SOLUTION_FILE: SimpleWorkerService.sln
      TESTS_FOLDER: SimpleWorkerService.Tests
      FORMAT_CHECKER_SEVERITY: "error"
      DOCKER_IMAGE_DETAILS: "{\"include\":[{\"path\":\"build/Dockerfile\",\"name\":\"simpleworkerservice\"}]}"
